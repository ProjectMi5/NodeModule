# Mi5 Node-Module (-Interface)

## Installation

`npm install`

Start the app in linux with the forever daemon:

`npm start`

## Development

Run the Tests:

`npm test`

Rebuild the documentation:

`npm run-script docs`
